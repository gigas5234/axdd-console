# Version Rule (Lite)

- Pack version: SemVer `MAJOR.MINOR.PATCH` in `metadata.version`
- `0.1.x` — Draft / ToyProject 검증 중
- Breaking change to handoff required_sections → MINOR bump + migration note
- `Draft` → `Accepted`: ToyProject 1회 사용 + reviewer 승인 + validation 통과
