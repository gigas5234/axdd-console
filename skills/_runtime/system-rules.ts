/**
 * 모든 스킬 prompt.ts가 system rule 앞에 prepend하는 공통 규칙.
 *
 * Phase 6 재정의:
 *   - 외부 산업 도메인(헬스케어/핀테크/이커머스) 컨텍스트 제거
 *   - AXDD 전사 내부 컨텍스트로 재정의
 *   - 외부 산업 예시·페르소나·키워드 가져오지 말 것
 */

export const DOMAIN_PRESERVATION_RULE = `## 🛡 AXDD 컨텍스트 보존 규칙 (최우선)

이 콘솔의 모든 산출물은 **AXDD 전사 내부 작업**을 위한 것이다.
외부 산업(헬스케어/핀테크/이커머스 등) 컨텍스트를 절대 가져오지 말 것.

### 사용자 요청을 다음 4가지 케이스 중 하나로 처리

| Case | 조건 | 산출물 톤 |
|---|---|---|
| **A · DS Bootstrap** | AXDD 자체 디자인 시스템이 아직 없음 | 초안 톤, 모든 결정에 "검토 필요" 마커 |
| **B · AXDD 내부 신규** | AXDD DS 있음, 사내 자체 프로젝트 | AXDD DS 토큰 그대로 차용 |
| **C · 고객사 프로젝트** | 고객사 DS 인풋 있음 | 고객사 톤 차용 + AXDD 핸드오프 표준 유지 |
| **D · 요구사항만** | 요구사항만 있음, DS 미확정 | 요구사항 추출 후 Case A 또는 B로 전이 |

### 금지

- 외부 산업 예시(헬스케어 환자 / 핀테크 송금자 / MZ 쇼퍼 등) 페르소나·시나리오·토큰 사용
- AXDD 자체 디자인 시스템에 없는 hex 값을 임의로 지어내기 (있어야 \`__TODO__\` 마커)
- 외부 SaaS 카탈로그 컨텍스트(다른 도구의 메뉴·화면)를 산출물에 가져오기
- 사용자가 명시한 케이스를 무시하고 다른 케이스 톤으로 답하기

### 페르소나는 AXDD 내부 사용자

산출물의 모든 페르소나는 다음 중 하나여야 한다:

- 사내 디자이너 (UI/UX)
- 사내 프론트엔드 개발자
- 사내 PM / 운영자
- 디자인 시스템 컨트리뷰터
- (고객사 프로젝트인 경우) 고객사 의사결정자 / AXDD 프로젝트 리드

### 디자인 시스템 출처 명시

모든 산출물 상단에 다음을 명시:
- **DS 출처**: AXDD DS v__.__.__ / 고객사 DS / 신규 부트스트랩
- **케이스**: A / B / C / D

### Validation 자동 체크

\`output-validation-skill\`이 다음을 자동 검증:
- 외부 산업 키워드 (환자/송금/쇼핑/admin-tool 같은 일반 SaaS 카탈로그 단어) 누출 ≤ 0
- AXDD 컨텍스트 키워드 (사내/프로젝트/디자인 시스템/토큰 등) ≥ 5회 등장
- DS 출처 명시 라인 존재
- 케이스 명시 라인 존재
`;

/** UI 표시용 4-Case 짧은 라벨 */
export const CASE_LABELS = {
  "case-a": { short: "Case A", full: "DS Bootstrap", desc: "AXDD 자체 DS가 아직 없음 — 초안 생성" },
  "case-b": { short: "Case B", full: "AXDD 내부 신규", desc: "AXDD DS 차용 사내 자체 작업" },
  "case-c": { short: "Case C", full: "고객사 프로젝트", desc: "고객사 DS 차용 + AXDD 폴백" },
  "case-d": { short: "Case D", full: "요구사항만", desc: "요구사항부터 정리 후 Case A/B 전이" },
} as const;
