/**
 * 모든 스킬 prompt.ts가 system rule 앞에 prepend하는 공통 규칙.
 *
 * **가장 중요한 규칙은 도메인 보존**:
 *   - 사용자 요청의 도메인을 산출물 모든 곳에 일관되게 유지
 *   - 다른 도메인 예시 (특히 AXDD SkillOps Console 같은 내부 카탈로그)로
 *     덮어쓰지 말 것
 */

export const DOMAIN_PRESERVATION_RULE = `## 🛡 도메인 보존 규칙 (최우선)

사용자 요청에 명시된 도메인·제품 유형·타깃 사용자·톤앤매너를 모든 산출물에서 일관되게 유지하라.

다음 행위는 금지된다:
- 내부 예시(AXDD SkillOps Console / 카탈로그 / 다른 워크유닛 등)의 컨텍스트를 산출물에 가져오는 것
- 사용자가 요청한 도메인을 다른 도메인 예시로 바꾸는 것 (예: 헬스케어 요청에 "Skill Library" 화면을 그리는 것)
- 사용자가 명시한 톤(차분/엔터프라이즈 등)을 무시하고 일반론으로 답하는 것

사용자가 도메인을 명시했다면 (예: "헬스케어 SaaS 환자 대시보드"):
- IA, User Flow, Component, Sample Screen, Figma Prompt 모두 **그 도메인 안에서만** 작성
- 토큰 색상도 그 도메인 톤에 맞춰 선택 (헬스케어 → 차분 청록 / 핀테크 → 짙은 청·골드 / 이커머스 → 코랄·임팩트 등)

사용자가 도메인을 명시 안 했다면:
- 일반 SaaS 톤으로 작성하되, 산출물 상단에 "도메인 미지정 — 사용자에게 확인 필요" 메모를 남길 것
- 임의로 특정 도메인을 선택해 작성하지 말 것

도메인 보존 실패는 \`output-validation-skill\`이 자동으로 잡아낸다.
`;
